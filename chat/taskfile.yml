version: "3"

tasks:
  generate:
    aliases:
      - gen-storage-client
    cmds:
      - mkdir -p pb
      - protoc -I proto proto/chat-storage.proto --go_out=./pb --go_opt=paths=source_relative --go-grpc_out=./pb --go-grpc_opt=paths=source_relative
      